server {
    listen 80;
    server_name {{ domain }};
    root {{ webRoot }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ /.well-known/acme-challenge/ {
        allow all;
        root {{ webRoot }};
    }
}
